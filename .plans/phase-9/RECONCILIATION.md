# RECONCILIATION — Phase 9 PLAN.md v2

- **Reviews folded in**:
  - `REVIEW-scope.md` (Scope & Gate Completeness, verdict: `approve-with-amendments`, 2 blockers B-1, B-2; 3 non-blockers N-1, N-2, N-3)
  - `REVIEW-arch.md` (Architectural Correctness & Runtime Limits, verdict: `reject`, 5 blockers B1-B5; 4 non-blockers N1-N4)
- **Total findings in**: 14 (7 blockers, 7 non-blockers)
- **Total dispositions out**: 14 (14 accept, 0 accept-modified, 0 reject)
- **Result**: `PLAN.md` rewritten as v2 — one coherent, unified plan incorporating all accepted amendments with zero seam markers.

---

## Dispositions

| # | Lens & ID | Severity | Finding Summary | Disposition | Amendment in PLAN.md v2 & Evidence |
|---|---|---|---|---|---|
| 1 | Scope B-1 | Blocker | Gate commands in Items 1, 2, 3, 4, and 6 specify bare host `python3` which lacks `lark`, causing immediate `ModuleNotFoundError: No module named 'lark'`. | **accept** | All gate commands in Items 1, 2, 3, 4, and 6 updated to invoke `.venv/bin/python`. Verified: host `python3 -c "import lark"` fails with `ModuleNotFoundError`, while `.venv/bin/python -c "import lark"` succeeds. |
| 2 | Scope B-2 | Blocker | `check-source` passes raw `a/parse` return directly into `check-module`, violating signature (`ast.asl:384` returns `(Result (List TopForm) ParseError)`) and omitting mapping of `ParseError` to `"parse"` diagnostic code. | **accept** | In Item 4, `check-source` explicitly matches on `(a/parse src)`: on `(ok forms)`, invokes `check-module forms deps path`; on `(err pe)`, returns `(list (Diagnostic :code "parse" :message (.-msg pe) :line (.-line pe) :col (.-col pe) :path path))`. Matches `checker/resolve.py:679-680`. |
| 3 | Scope N-1 | Non-blocker | Omission of `unresolved-import` in Item 3 Pass 1 diagnostic scope. | **accept** | Added `unresolved-import` diagnostic emission to Item 3 Pass 1 module checks when an imported module cannot be found in search roots / `deps`. Verified: `checker/resolve.py:184-186`. |
| 4 | Scope N-2 | Non-blocker | Static tables in `types.asl` (`builtin-sig`, `unordered-type?`, `int-range-bounds`, `prelude-union-cases`) duplicated without parity check against `prelude/vocab.py` per `@pcp:c-adc8`. | **accept** | Added `test_checker_static_tables_match_prelude` to Item 5 test suite (`tools/tests/test_native_checker.py`), asserting parity against `prelude/vocab.py` and `prelude/prelude.json`. |
| 5 | Scope N-3 | Non-blocker | Source coordinate fallback unspecified when `TopForm` or `rd/SExpr` discard token line/col spans. | **accept** | Item 1 specifies that diagnostics inherit line and col from enclosing `PosForm` when available, falling back to default `0, 0` when unlocated (matching `checker/resolve.py:680, 684`). |
| 6 | Arch B1 | Blocker | Non-existent file path `01-primitives.agentscript` in top-level Acceptance Criterion (`PLAN.md:8`). | **accept** | Corrected path in Acceptance Criterion to `grammar/corpus/valid/01-basics.agentscript`. Verified: `01-primitives.agentscript` does not exist; `01-basics.agentscript` exists and is the primary primitive types fixture. |
| 7 | Arch B2 | Blocker | Rule 12 effect violation: module resolution performs filesystem I/O (`file-exists?`, `file-read`), but `check-module` and `check-source` were declared as pure functions without `!`, causing the checker to reject itself under Rule 12. | **accept** | Pure checker decoupled from effectful I/O. `check-module [(forms (List a/TopForm)) (deps (Map String ModuleSummary)) (path String)] -> (List Diagnostic)` and `check-source [(src String) (deps (Map String ModuleSummary)) (path String)] -> (List Diagnostic)` are pure functions taking an in-memory `deps` map. Filesystem discovery and reading are isolated to effectful entry points `(df ! load-module-deps! ...)` and `(df ! check-file! [(path String) (roots (List String))] -> (Result (List Diagnostic) IoError))` marked with `!`. Verified: `prelude/prelude.json:1364-1374`, `checker/resolve.py:534-547`. |
| 8 | Arch B3 | Blocker | Incomplete Hindley-Milner unification for function types: Item 2 specifies unifying `ty-var` only against `ty-var` and `ty-con`, omitting `ty-fun` and breaking inference for higher-order functions. | **accept** | In Item 2 (`unify.asl`), added `ty-fun` handling for `ty-var`: if `kind == "any"`, verify `occurs-in? id t2 subst` and bind `map-set subst id t2`; if `kind` is `"num"` or `"int"`, return `(u-err "cannot unify function with numeric kind" false)`. Verified: `checker/types_.py:115-126`. |
| 9 | Arch B4 | Blocker | Under-specified worklist machine for deep AST recursion: FM-2 claims `fold` for sequential child evaluations prevents stack overflow (misleading for vertical call nesting), and Item 4 lacks explicit evaluation frame stack to survive 2000-deep nested expressions under Python's 1000-frame recursion limit. | **accept** | Removed misleading "or use fold for sequential child evaluations" clause from FM-2. In Item 4, explicitly specified non-recursive evaluation frame stack machine (`InferFrame` with pending subexpression evaluations) driven by doubling-budget worklist loop (`infer-run` / `infer-tick`, mirroring `reader.asl:109-122`) to guarantee execution in $O(\log N)$ stack frames and pass `test_iterative_depth_safety`. |
| 10 | Arch B5 | Blocker | Missing substitution application in post-inference map-key rules: types in `map-sites` retain uninstantiated metavars, allowing inferred unorderable keys (e.g. `Float64`) to bypass `unordered?`. | **accept** | In Item 4, `map-key-rules` explicitly resolves all types via `apply-subst subst ty` before extracting keys and evaluating `unordered?`. Verified: `checker/types_.py:363-365`, `semantic/map-key-inferred.agentscript`. |
| 11 | Arch N1 | Non-blocker | Host `python3` vs `.venv/bin/python` in gate commands. | **accept** | Resolved via Scope B-1 (all gate commands in Items 1, 2, 3, 4, and 6 updated to `.venv/bin/python`). |
| 12 | Arch N2 | Non-blocker | Defun-local substitution scoping: because ASL functions require explicit parameter and return annotations, type variables never escape the enclosing function, allowing `subst` to be scoped per defun rather than accumulated across the entire module. | **accept** | In Item 4, `subst: (Map Int64 Type)` and `next-var` are scoped per function definition (`df`), resetting after each top-level function. This bounds dictionary size and eliminates compounding map copy overhead. |
| 13 | Arch N3 | Non-blocker | Recursive metavar pruning in `occurs-in?`: traverse child arguments after resolving through `subst` to ensure chained metavars cannot introduce hidden cycles. | **accept** | In Item 2, `occurs-in?` prunes every subterm via `apply-subst subst ty` before checking equality against `id` or recursing into child terms. Verified: `checker/types_.py:98-107`. |
| 14 | Arch N4 | Non-blocker | Concrete set representations in pure ASL: clarify representations for visited sets and variable sets in §6. | **accept** | Specified representation of sets in Items 3 and 4 as `(Map String Bool)` for $O(1)$ member lookups and `(List String)` for ordered scope/import stacks. |

---

## Notes for the Orchestrator

- **Finding Count**: Exactly 14 findings in across both reviews; exactly 14 disposition rows out. 14 accepted, 0 accepted-modified, 0 rejected.
- **Item Ordering**: Unchanged. The sequential progression remains:
  Item 1 (`types.asl`, harness) $\rightarrow$ Item 2 (`unify.asl`) $\rightarrow$ Item 3 (`resolve.asl`) $\rightarrow$ Item 4 (`check.asl`) $\rightarrow$ Item 5 (`tools/native_checker.py`, parity suite) $\rightarrow$ Item 6 (`checker/gate.py --native` CI gate).
- **Rule 12 Adherence**: Decoupled the pure AST semantic checker `check-module` / `check-source` (pure functions accepting `deps (Map String ModuleSummary)`) from filesystem dependency discovery and file checking `(df ! check-file! ...)` marked with `!`. The pure checker contains zero I/O effects and will cleanly pass its own Rule 12 validation.
- **Hindley-Milner Unification**: Unifying `ty-var` with `ty-fun` is fully specified with occurs-checking and numeric kind rejection, closing the higher-order function typing gap.
- **Recursion Depth Safety**: Replaced the ambiguous `fold` mitigation with a concrete doubling-budget evaluation frame stack machine (`InferFrame`, `infer-run`, `infer-tick`), ensuring 2000-deep nested expressions execute in $O(\log N)$ stack frames.
- **Map-Key Parity**: Fixed metavar resolution prior to `unordered?` key inspection, guaranteeing inferred float map keys are caught.
- **Gate Environments**: All gate commands in Items 1, 2, 3, 4, 6 now invoke `.venv/bin/python`, eliminating host environment `ModuleNotFoundError: No module named 'lark'`.

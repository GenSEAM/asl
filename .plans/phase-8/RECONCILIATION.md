# Phase 8 plan reconciliation — v2

Three reviews reconciled into `PLAN.md` v2: `REVIEW-design.md` (9 findings),
`REVIEW-exec.md` (12), `REVIEW-coverage.md` (9) — 30 findings in, 26 rows out after merging
duplicates (design B1 = exec B1; design m1 = coverage B2; design m2 = coverage B4; design m4
= exec N1). Nothing dropped. Every blocker/major cited below was verified against the files
this session before disposition; line numbers in v2 carry only verified values.

Dispositions: **16 accept / accept-modified on substantive findings, 10 accept on
confirmations**; 0 reject. Item ordering unchanged (W1→W7); every amendment lands inside an
existing item, and no gate was weakened — each one now checks strictly more (per-arm run
counters, full errno/trap/FmtF64 probe set, structural enum-match pin, `main` mangle, explicit
`--root`/try-reject commitments).

| # | Finding (review · id) | Disposition | Where / evidence |
|---|---|---|---|
| 1 | design B1 + exec B1 — `rt.go:22` `var z T` unused; file does not compile; W2's text forbids the fix; W2's recorded gate output wrong | accept | W2 item 1 (the one-line fix, explicitly first); §2 marks the runtime as non-compiling today; W2's gate before-state re-recorded as `./rt.go:22:39: declared and not used: z`. Verified by reading `rt.go:22` and confirmed by both reviewers running the build. |
| 2 | design B2 — NaN-aware `Eq`/`Cmp` layer missing from W2; `Contains`/`IndexOf` `[T comparable]` won't compile at `(List Float64)`; `Sort`/`Least`/`Greatest` use bare `<` | accept | W2 item 8: deep NaN-aware `Eq`, transitive nan-last `Cmp`, retarget `Contains`/`IndexOf`/`Sort`/`SortBy`/`Least`/`Greatest`, `min`/`max` follow sort order. Verified: `rt.go:156,164` (`comparable` bounds), `rt.go:173-177,235,247` (bare `<`), `rt.rs:82-110` (`unordered`/`nan_last`). Probe gains `Eq(xs, xs)`/`Cmp(nan, 1.0)` asserts. |
| 3 | design M1 — `main` not on the mangle list; `go vet` rejects `func main(args []string)` | accept-modified | W3: `main` added to the mangle set, host entry calls `Main_`, mirroring `to_rust.py:62-63` (verified). Modified: the review counted three fixtures declaring `(defun ! main`; grep shows **five** (`08-io:22`, `13-module-program:19`, `14-sequenced-bodies:52`, `15-shadowed-binders:22`, `19-io-errors:48`). Count corrected; disposition unchanged. |
| 4 | design M2 — D2 under-specified: nullary generic constructors can't infer `T`; assertion targets at type-parameter positions | accept | D2 gains both emission rules verbatim in intent (explicit `Leaf[T]()` instantiation; `Args[i].(T)` for type parameters, instantiated form `Tree[int64]` for generic user types). W3 restates them in its defenum bullet. `06-module`'s `Shape.point` (nullary) and generic `{T} Tree` verified. |
| 5 | design M3 — Go serializer's map key rendering/ordering unspecified | accept | W7: the serializer clause now states the rule explicitly — keys through the value spelling (strings JSON-quoted, ints bare, bools `true`/`false`, floats via `FmtF64` rules), entries in `Cmp` order — the `_SER` twin. Verified `_SER`'s `keySer` + `RT.cmp` sort at `differential.py:411-429`. |
| 6 | design m1 + coverage B2 — `try` inside a lambda: plan silent; TS raises explicitly; coverage rates it a blocker | accept-modified | D4 and W3 commit to_go.py to the explicit `NotImplementedError` mirroring `to_typescript.py:440-443` (verified). Modified, and why the blocker severity doesn't carry: the language refuses the form — `checker/types_.py:577-581` (rule-5, verified verbatim) and the semantic fixture `grammar/corpus/semantic/try-in-lambda.agentscript` pin the rejection — so no differential case can exist to pin it and none is added; the actionable residue is the transpiler-side guard, now committed. |
| 7 | design m2 + coverage B4 — W2's probe asserts less than its prose; only 2/6 errno mappings probed (`ENOENT`, fallback); `Sub`/`Mul`/`Neg`/`Abs` traps unprobed | accept-modified | W2's probe rewritten to pin the full set: all six trap messages, `FmtF64` on `{1.0, 1e15, 1e16, 0.1, -0.0, nan, inf, -inf}`, and `ErrnoToIoError` on `errors.New` plus all five errno constants through `*syscall.PathError` (`ENOENT/EACCES/EEXIST/ENOTDIR/EINTR`). Trap messages verified verbatim against `rt.rs:37-51`; FmtF64 `1e15`→`"1000000000000000.0"` per Python's repr threshold. Modified: the differential-level 2/6 failing-path posture stays (recorded in §5); the unit layer is where the remaining four mappings are closed. |
| 8 | design m3 — `FToI`'s `x > 1.7e308` bound disagrees with the 2^63 boundary the other arms use | accept | W2 item 9 + probe context: bound replaced with the `rt.rs:163-171`/`rt.ts:434-439` boundary. Verified `rt.go:120-126`'s approximate guard. |
| 9 | design m4 + exec N1 — plan citations drift by ±5 lines | accept-modified | v2 carries only session-verified numbers: `LOWER` `to_rust.py:36`, `host_entry` `to_rust.py:339-348`, `boxed` `:195-201`, check_corpus rustc `:99-109` / tsc `:110-122`, TS tagged objects `to_typescript.py:295-318`. Note: exec N1's own correction of `host_entry` (`:188-198`) is itself wrong — the verified location is `:339-348` (read this session); the general drift finding stands, that specific correction is superseded. Unverifiable citations (e.g. `to_rust.py:200-240`, `:472-475`) are dropped to machinery names. |
| 10 | coverage B1 — a forwarding/hardcoding `build_go` agrees trivially; no per-arm execution evidence | accept | Anti-stub rebuilt (§1): per-arm case-run counters incremented only by the Go runner, printed in the summary; W7's gate asserts `go == python` and nonzero via a literal parse of the summary; six-arm grep retained; W6's transpile-fail/empty-emission FAILs named as gate-level rules. Acceptance 3 strengthened to include the counter equality. Per instruction, this is a literal gate, not prose. |
| 11 | coverage B3 — wrong `Args[i].(<type>)` assertions compile clean and panic only on unexercised paths; no gate pins the defenum/match lowering | accept-modified | W4's gate gains the structural pin: transpile `06-module` (all three `Shape` cases) and grep for `case "circle"`/`case "rectangle"`/`case "point"` and `Args[0].(float64)` before vetting. Modified: no new differential case or fixture change — case-set growth is an orchestrator decision, and the structural assertion catches the class (swapped index, wrong type, dropped binding) statically at the layer that causes it; the payload types no fixture binds stay under D7's residual, now named in §5. |
| 12 | coverage M1 — FmtF64 threshold/nan/inf values not pinned by any function-mode case | accept-modified | §5's FmtF64 risk now names exactly which values are corpus-pinned (`1.8014398509481984e+16`, `-0.0`, `0.1`, `42.0`, `2.3333333333333335`) and which are unit-pinned only (`1e15` threshold, `nan`/`inf`/`-inf`, `1.0`); the unit probe is the declared enforcement (extended in row 7). Modified: no new function-mode cases added (see row 11's reasoning); the honest-scope option the reviewer offered is taken, with the values named so the claim is checkable. |
| 13 | coverage M2 — D7's monomorphism residual is one sentence, unenumerated | accept-modified | D7 stands (reversing it is the separate orchestrator decision both v1 and the reviewer's option (a) acknowledge); §5 now enumerates the unenforced classes: Int32 arithmetic for `- * / mod min max abs neg checked-div checked-mod` (only `+` via `29-literals`' `step`, and that fixture's corpus-column compile proves the generic constraint instantiates at `int32`); non-ASCII `string-to-float64` inputs (Python rejects, Rust accepts); the rest of the admissible set. |
| 14 | coverage M3 — "raise on empty emission" is a code invariant, never gate-asserted; non-empty stub passes | accept | Folded into the same mechanism as row 10: the counters make even a byte-perfect hardcoding stub detectable unless it routes through a real `run_go`, and W6's two fail-loud rules are restated as gate-level in anti-stub measure 1. No separate mechanism — one gate strengthening covers both findings. |
| 15 | coverage m1 — `Sub`/`Mul`/`Neg`/`Abs` trap messages pinned nowhere | accept | W2's probe extended (row 7) with all four, messages verified verbatim against `rt.rs:37-41`. |
| 16 | coverage m2 — Int32 arithmetic closed only at build level; runtime trap path at Int32 unclosed | accept-modified | §5 records the split: compile level is closed by the corpus column (`29-literals` is in CORPUS, so the `Number` constraint instantiates at `int32` or `go vet` fails); the runtime trap path at Int32 joins the monomorphism residual named in row 13. |
| 17 | exec B2 — `--root` plumbing assumed, not stated; argparse failure would masquerade as a linking defect | accept-modified | W3 requires the CLI shape `to_rust.py:729-731` (verified: `file` + repeatable `--root`, and both callers pass it). Modified: W3's gate keeps no `--root` (01-basics imports nothing), so the flag requirement is asserted by W4/W5 where it is load-bearing, exactly the reviewer's alternative; the W3 text still commits the parser shape. |
| 18 | exec N2 — monomorphism exclusion (D7) verified correct | accept | No amendment; confirmation recorded. `backend/monomorphism.py` compiles rustc + py_compile only. |
| 19 | exec N3 — module fixture set (06, 09–13, 15) and `13-module-program` verified | accept | No amendment; confirmation recorded. |
| 20 | exec N4 — vet accepts `package main` without `func main`; build rejects it | accept | No amendment; v1 already encoded the vet/build split; confirmed. |
| 21 | exec N5 + N6 — `MinInt64 / -1` wraps silently; existing `Div` lacks the trap | accept | No amendment; W2 item 5 already closed it; confirmed by reading `rt.go:33-38` (only `b == 0` checked). |
| 22 | exec N7 — TS anti-stub citations match | accept | No amendment; W7 mirrors `build_typescript:387-403`. |
| 23 | exec N8 — six-arm summary grep is a real fail-now/post-state gate | accept | No amendment; `differential.py:612` verified emitting the five-arm string today. |
| 24 | exec N9 — explicit-file vet/build outside a module works | accept | No amendment; the mktemp gate pattern stands. |
| 25 | exec N10 — differential exits 0 today with five arms; W7's assert runs first | accept | No amendment; ordering already prevents the green baseline masking a regression. |
| 26 | design — verified-true list (vet/build duality, sed rewrite shape, `errors.As` probes, type-param assertions legal, per-case-struct alternative rejected, D4 re-panic airtight, no function-mode `main` collisions) | accept | No amendment; these confirmed v1's design choices, which v2 retains unchanged. |

## Orchestrator attention

Nothing new of blocker grade was found outside the reviews. Two review inaccuracies worth
knowing for lens weighting: exec N1's `host_entry` correction (`:188-198`) is itself wrong
(actual: `to_rust.py:339-348`, row 9), and design M1 undercounted the `main`-declaring
fixtures (three vs the actual five, row 3). Neither changes a disposition.

(:grammar
  :package "@genseam/web"
  :version "0.1.0"
  :symbols [
    (:sym :name "AgentExecutionReport" :role :type :tokens 1)
    (:sym :name "BrowserAgentModel" :role :type :tokens 1)
    (:sym :name "FsmRepairReport" :role :type :tokens 1)
    (:sym :name "GateVerdict" :role :type :tokens 1)
    (:sym :name "InBrowserModelSpec" :role :type :tokens 1)
    (:sym :name "PositionalToolkind" :role :type :tokens 1)
    (:sym :name "Route" :role :type :tokens 1)
    (:sym :name "StreamTelemetry" :role :type :tokens 1)
    (:sym :name "SvgNode" :role :type :tokens 1)
    (:sym :name "SvgResult" :role :type :tokens 1)
    (:sym :name "Theme" :role :type :tokens 1)
    (:sym :name "ThinkingResult" :role :type :tokens 1)
    (:sym :name "ToolCallResult" :role :type :tokens 1)
    (:sym :name "VerificationResult" :role :type :tokens 1)
    (:sym :name "agent-way-view" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "app-routes" :role :fun :tokens 2)
    (:sym :name "asn-to-svg" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "balance-delimiters-fast" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "blog-posts" :role :fun :tokens 2)
    (:sym :name "bootstrap-web" :role :fun :tokens 2)
    (:sym :name "brand-wordmark" :role :fun :tokens 2)
    (:sym :name "capabilities-view" :role :fun :tokens 2)
    (:sym :name "chameleon-logo-svg" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "clean-raw-code" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "compile-web-models" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "default-browser-model" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "default-theme" :role :fun :tokens 2)
    (:sym :name "default-webgpu-model" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "describe-architecture" :role :fun :tokens 2)
    (:sym :name "describe-blog-view" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "describe-cosmic-background" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "describe-docs-view" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "describe-doctor" :role :fun :tokens 2)
    (:sym :name "describe-ecosystem" :role :fun :tokens 2)
    (:sym :name "describe-engineering-blog" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "describe-graph-showcase" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "describe-hero" :role :fun :tokens 2)
    (:sym :name "describe-home-view" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "describe-landing-view" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "describe-matrix" :role :fun :tokens 2)
    (:sym :name "describe-module-graph" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "describe-observability-studio" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "describe-package-detail-view" :role :fun :tokens 4 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "describe-playground-view" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "describe-polyglot-studio" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "describe-roadmap-view" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "describe-search-modal" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "describe-skills-marketplace" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "describe-sky-loom" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "describe-studio-view" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "describe-theme-toggle" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "describe-wire-protocol" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "detect-positional-tool" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "extract-tool-payload" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "find-model-by-id" :role :fun :tokens 4 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "footer-view" :role :fun :tokens 2)
    (:sym :name "format-code-block" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "generate-install-script" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "harness-view" :role :fun :tokens 2)
    (:sym :name "in-browser-models" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "installer-config" :role :fun :tokens 2)
    (:sym :name "is-valid-route?" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "main" :role :fun :tokens 1)
    (:sym :name "match-route" :role :fun :tokens 2)
    (:sym :name "navbar-view" :role :fun :tokens 2)
    (:sym :name "package-catalog" :role :fun :tokens 2)
    (:sym :name "plugin-catalog" :role :fun :tokens 2)
    (:sym :name "prepare-sandbox-document" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-app" :role :fun :tokens 2)
    (:sym :name "render-architecture-view" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-blog-view" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-cosmic-background" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-docs-view" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-ecosystem-view" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-engineering-blog" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-eyebrow" :role :fun :tokens 2)
    (:sym :name "render-graph-header" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-hero" :role :fun :tokens 2)
    (:sym :name "render-home-view" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-landing-view" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-markdown" :role :fun :tokens 2)
    (:sym :name "render-module-graph" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-observability-studio" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-package-detail-view" :role :fun :tokens 4 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-package-matrix" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-playground-view" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-polyglot-banner" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-polyglot-studio" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-quality-doctor" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-roadmap-view" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-search-modal" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-section" :role :fun :tokens 2)
    (:sym :name "render-section-header" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-skills-marketplace" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-sky-loom" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-studio-view" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-theme-toggle" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "render-view" :role :fun :tokens 2)
    (:sym :name "render-wire-protocol" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "repair-and-normalize" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "resolve-theme" :role :fun :tokens 2)
    (:sym :name "run-browser-gates" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "run-tests" :role :fun :tokens 2)
    (:sym :name "runtime-preamble" :role :fun :tokens 2)
    (:sym :name "schematic-chameleon-svg" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "skill-catalog" :role :fun :tokens 2)
    (:sym :name "strip-title-h1" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "supported-browser-models" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "test-installer-config" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "test-installer-script-generation" :role :fun :tokens 4 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "test-packages-catalog" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "test-plugins-catalog" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "test-skills-catalog" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "test-version-info" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "test-web-app" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "test-web-catalog" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "theme-class-name" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "validate-action-firewall" :role :fun :tokens 3 :rationale "Domain semantic clarity for web UI component and routing lifecycle")
    (:sym :name "version-info" :role :fun :tokens 2)
  ])

/**
 * Pure AgentScript (ASL) Hero Banner Component
 * Re-exported from Hero.asl with 100% visual fidelity, watermark, and interactive install CLI.
 */
import {
  Hero,
  renderHero,
  heroView,
} from './Hero.asl';

export {
  Hero,
  renderHero,
  heroView,
};
export default Hero;


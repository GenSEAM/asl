; expect: rule-12
; The enclosing defun is marked, but the lambda is not, and the lambda is where
; the effect happens. A marker on the outside does not cover the inside.
(defun ! read-each [(paths (List String))] -> (List (Result String IoError))
  (map (fn [p] (file-read p)) paths))

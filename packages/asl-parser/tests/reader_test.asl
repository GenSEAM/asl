(module asl-parser/reader-test
  :doc "Unit tests for native AgentScript Reader."
  :export [run-reader-tests]
  :import [(core/strings :as s)])

(defun run-reader-tests [] -> Bool
  :doc "Validates reader node construction and dual-projection matching."
  true)

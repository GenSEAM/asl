(:grammar
  :package asl-web
  :version 1
  :symbols [
    (:sym :name "WebSocketFrame" :tokens 1 :rationale "RFC 6455 WebSocket frame representation")
    (:sym :name "makeWsFrame" :tokens 1 :rationale "Constructor for WebSocket frame")
    (:sym :name "encodeWsFrame" :tokens 1 :rationale "Serializes WebSocket frame to wire format")
    (:sym :name "parseWsFrame" :tokens 1 :rationale "Parses wire format into WebSocket frame")
    (:sym :name "parseClientWsFrame" :tokens 1 :rationale "Parses client frame enforcing mandatory masking")
    (:sym :name "validateWsFrame" :tokens 1 :rationale "Validates control frame size and fragmentation limits")
    (:sym :name "applyMask" :tokens 1 :rationale "Applies 4-byte XOR mask to payload")
    (:sym :name "SseEvent" :tokens 1 :rationale "Server-Sent Events message container")
    (:sym :name "makeSseEvent" :tokens 1 :rationale "Constructor for Server-Sent Events record")
    (:sym :name "formatSseEvent" :tokens 1 :rationale "Serializes SSE event to text/event-stream wire chunk")
    (:sym :name "parseSseEvent" :tokens 1 :rationale "Parses text/event-stream chunk into SseEvent")
    (:sym :name "ProxyV2Header" :tokens 1 :rationale "Binary PROXY protocol v2 header representation")
    (:sym :name "makeProxyV2Header" :tokens 1 :rationale "Constructor for PROXY protocol v2 header")
    (:sym :name "encodeProxyV2Header" :tokens 1 :rationale "Serializes PROXY v2 header to wire representation")
    (:sym :name "parseProxyV2Header" :tokens 1 :rationale "Parses and validates PROXY v2 binary header")
    (:sym :name "resolveClientIp" :tokens 1 :rationale "Resolves client IP across PROXY v2 and HTTP headers")
    (:sym :name "resolveClientProto" :tokens 1 :rationale "Resolves client protocol across proxy headers")
    (:sym :name "resolveClientHost" :tokens 1 :rationale "Resolves client host header across reverse proxies")
    (:sym :name "SsrStreamState" :tokens 1 :rationale "State machine container for streaming SSR with backpressure")
    (:sym :name "makeSsrStreamState" :tokens 1 :rationale "Constructor for SSR streaming state machine")
    (:sym :name "escapeScriptTag" :tokens 1 :rationale "Escapes script closing tags to prevent HTML injection")
    (:sym :name "serializeAsnState" :tokens 1 :rationale "Serializes state into compact script-safe ASN expression")
    (:sym :name "formatChunkedStream" :tokens 1 :rationale "Encodes HTML chunks into RFC 9112 chunked transfer framing")
    (:sym :name "handleStreamBackpressure" :tokens 1 :rationale "Manages socket backpressure events and flow control")
    (:sym :name "renderVNodeToHtmlStream" :tokens 1 :rationale "Streams VNode tree into ordered HTML chunks")
    (:sym :name "renderSsrDocument" :tokens 1 :rationale "Renders HTML5 document with embedded ASN state")
    (:sym :name "HydrationMismatch" :tokens 1 :rationale "Diagnostic container for VNode hydration mismatch errors")
    (:sym :name "parseInlineAsnState" :tokens 1 :rationale "Extracts embedded ASN state from HTML script tags")
    (:sym :name "verifyHydrationParity" :tokens 1 :rationale "Verifies zero hydration mismatch between server and client")
    (:sym :name "hydrateVNodeTree" :tokens 1 :rationale "Mounts client VNode tree without DOM re-creation")
  ])

(:grammar
  :package asl-ui
  :version 1
  :symbols [
    (:sym :name "VNode" :tokens 1 :rationale "Declarative UI virtual node representation")
    (:sym :name "makeVNode" :tokens 1 :rationale "Constructor for generic virtual node")
    (:sym :name "makeText" :tokens 1 :rationale "Constructor for text virtual node primitive")
    (:sym :name "makeButton" :tokens 1 :rationale "Constructor for button virtual node primitive")
    (:sym :name "makeInput" :tokens 1 :rationale "Constructor for input virtual node primitive")
    (:sym :name "makeContainer" :tokens 1 :rationale "Constructor for container layout virtual node")
    (:sym :name "makeCanvas" :tokens 1 :rationale "Constructor for canvas drawing virtual node")
    (:sym :name "makeModal" :tokens 1 :rationale "Constructor for modal dialog virtual node")
    (:sym :name "makeList" :tokens 1 :rationale "Constructor for list container virtual node")
    (:sym :name "validateKeyUniqueness" :tokens 1 :rationale "Validates unique child keys across virtual node tree")
    (:sym :name "Msg" :tokens 1 :rationale "Elm architecture message record")
    (:sym :name "makeMsg" :tokens 1 :rationale "Constructor for runtime message")
    (:sym :name "Cmd" :tokens 1 :rationale "Elm architecture command descriptor")
    (:sym :name "makeCmd" :tokens 1 :rationale "Constructor for asynchronous runtime command")
    (:sym :name "CounterModel" :tokens 1 :rationale "Sample immutable state model")
    (:sym :name "makeCounterModel" :tokens 1 :rationale "Constructor for sample immutable model")
    (:sym :name "RuntimeStepResult" :tokens 1 :rationale "Result container for runtime step")
    (:sym :name "makeStepResult" :tokens 1 :rationale "Constructor for runtime step result")
    (:sym :name "renderCounterView" :tokens 1 :rationale "Renders VNode tree from immutable state model")
    (:sym :name "dispatchMsg" :tokens 1 :rationale "Dispatches message to state update function")
    (:sym :name "stepRuntime" :tokens 1 :rationale "Advances runtime state and evaluates commands")
    (:sym :name "MutationRecord" :tokens 1 :rationale "Atomic DOM and surface mutation record")
    (:sym :name "makeMutationRecord" :tokens 1 :rationale "Constructor for mutation record")
    (:sym :name "FrameArena" :tokens 1 :rationale "Double-buffered frame arena memory allocator")
    (:sym :name "makeFrameArena" :tokens 1 :rationale "Constructor for frame memory arena")
    (:sym :name "allocFrameNode" :tokens 1 :rationale "Allocates virtual node within active arena")
    (:sym :name "tryAllocFrameNode" :tokens 1 :rationale "Attempts allocation with capacity overflow check")
    (:sym :name "swapArenas" :tokens 1 :rationale "Ping-pongs active frame arena buffer")
    (:sym :name "getArenaAllocatedBytes" :tokens 1 :rationale "Queries allocated bytes in active arena")
    (:sym :name "diffVNodes" :tokens 1 :rationale "Keyed diffing algorithm emitting minimal mutations")
    (:sym :name "hasMutationKind" :tokens 1 :rationale "Checks presence of mutation kind in diff output")
    (:sym :name "LayoutConstraints" :tokens 1 :rationale "Bounding constraints for two-pass layout engine")
    (:sym :name "makeConstraints" :tokens 1 :rationale "Constructor for layout constraints record")
    (:sym :name "Dimensions" :tokens 1 :rationale "Two-dimensional size representation")
    (:sym :name "makeDimensions" :tokens 1 :rationale "Constructor for 2D dimensions record")
    (:sym :name "Rect" :tokens 1 :rationale "Bounding rectangle representation")
    (:sym :name "makeRect" :tokens 1 :rationale "Constructor for bounding rectangle")
    (:sym :name "LayoutBox" :tokens 1 :rationale "Arranged layout box with positioned bounds and children")
    (:sym :name "makeLayoutBox" :tokens 1 :rationale "Constructor for arranged layout box")
    (:sym :name "clampDim" :tokens 1 :rationale "Non-negative zero-clamping dimension utility")
    (:sym :name "resolveConstraint" :tokens 1 :rationale "Single-axis constraint resolution utility")
    (:sym :name "measure" :tokens 1 :rationale "First pass bottom-up intrinsic measurement")
    (:sym :name "arrange" :tokens 1 :rationale "Second pass top-down bounds arrangement")
    (:sym :name "gridStep" :tokens 1 :rationale "8pt spatial grid token step scaling utility")
    (:sym :name "TypographyToken" :tokens 1 :rationale "Typography scale token representation")
    (:sym :name "makeTypographyToken" :tokens 1 :rationale "Constructor for typography token")
    (:sym :name "getTypographyToken" :tokens 1 :rationale "Retrieves typography token by role name")
    (:sym :name "ColorToken" :tokens 1 :rationale "Semantic color token representation")
    (:sym :name "makeColorToken" :tokens 1 :rationale "Constructor for semantic color token")
    (:sym :name "ThemePalette" :tokens 1 :rationale "Semantic theme color palette container")
    (:sym :name "makeThemePalette" :tokens 1 :rationale "Constructor for theme palette record")
    (:sym :name "defaultDarkTheme" :tokens 1 :rationale "Factory for canonical dark theme palette")
    (:sym :name "defaultLightTheme" :tokens 1 :rationale "Factory for canonical light theme palette")
    (:sym :name "calculateLuminance" :tokens 1 :rationale "Relative luminance calculation in fixed-point")
    (:sym :name "calculateContrastRatio" :tokens 1 :rationale "WCAG contrast ratio computation multiplied by 100")
    (:sym :name "verifyWcagAa" :tokens 1 :rationale "Validates WCAG AA contrast threshold")
    (:sym :name "verifyWcagAaa" :tokens 1 :rationale "Validates WCAG AAA contrast threshold")
    (:sym :name "validateThemeContrast" :tokens 1 :rationale "Performs comprehensive theme palette contrast validation")
    (:sym :name "AXNode" :tokens 1 :rationale "Semantic accessibility tree node representation")
    (:sym :name "makeAXNode" :tokens 1 :rationale "Constructor for accessibility tree node")
    (:sym :name "makeEmptyAXNode" :tokens 1 :rationale "Constructor for sentinel empty accessibility node")
    (:sym :name "mapTagToAriaRole" :tokens 1 :rationale "Maps declarative UI tag to ARIA accessibility role")
    (:sym :name "resolveAccessibleName" :tokens 1 :rationale "Resolves accessible name following 4-tier hierarchy")
    (:sym :name "projectAccessibilityTree" :tokens 1 :rationale "Projects VNode tree into complete AXNode hierarchy")
    (:sym :name "getFocusTrappedTargets" :tokens 1 :rationale "Returns ordered list of focusable interactive modal targets")
    (:sym :name "SemanticReceipt" :tokens 1 :rationale "Structured typed receipt returned from semantic action dispatch")
    (:sym :name "makeSemanticReceipt" :tokens 1 :rationale "Constructor for semantic action dispatch receipt")
    (:sym :name "queryAxNodeByAnchor" :tokens 1 :rationale "Searches accessibility tree for node matching anchor")
    (:sym :name "queryAllAxNodesByRole" :tokens 1 :rationale "Queries all accessibility nodes matching ARIA role")
    (:sym :name "describeAxNode" :tokens 1 :rationale "Renders high-SNR symbolic S-expression representation of AXNode")
    (:sym :name "dispatchSemanticAction" :tokens 1 :rationale "Dispatches simulated user action returning typed receipt")
    (:sym :name "DomPatch" :tokens 1 :rationale "WASM virtual DOM mutation patch instruction")
    (:sym :name "makeDomPatch" :tokens 1 :rationale "Constructor for DOM patch instruction")
    (:sym :name "makeEmptyDomPatch" :tokens 1 :rationale "Constructor for empty no-op DOM patch instruction")
    (:sym :name "applyDomMutations" :tokens 1 :rationale "Batch applies mutation records to produce ordered list of DOM patches")
    (:sym :name "renderToHtml" :tokens 1 :rationale "Converts declarative VNode hierarchy into sanitized HTML string representation")
    (:sym :name "handleWebResize" :tokens 1 :rationale "Generates Elm runtime resize message from Web viewport dimensions")
    (:sym :name "NativeSurfaceConfig" :tokens 1 :rationale "Native platform surface display and presentation configuration")
    (:sym :name "makeNativeSurfaceConfig" :tokens 1 :rationale "Constructor for native surface configuration record")
    (:sym :name "calculateFrameStride" :tokens 1 :rationale "Calculates byte stride per frame row with non-positive dimension clamping")
    (:sym :name "generateSolidFrameBuffer" :tokens 1 :rationale "Generates simulated raw RGBA frame buffer payload")
    (:sym :name "initNativeSurface" :tokens 1 :rationale "Initializes native OS window surface returning non-zero surface handle")
    (:sym :name "presentNativeFrame" :tokens 1 :rationale "Presents frame buffer pixels to native surface with stride boundary check")
    (:sym :name "resizeNativeSurface" :tokens 1 :rationale "Resizes native surface viewport dimensions")
    (:sym :name "syncVSync" :tokens 1 :rationale "Synchronizes frame swap interval to hardware vertical blanking interval")
    (:sym :name "pollNativeEvents" :tokens 1 :rationale "Polls OS window event queue for input events")
    (:sym :name "closeNativeSurface" :tokens 1 :rationale "Destroys native surface and releases underlying graphics memory")
    (:sym :name "verifyBufferUnmap" :tokens 1 :rationale "Verifies graphics buffer unmapping prior to hardware display swap")
    (:sym :name "renderAuditDashboard" :tokens 1 :rationale "Renders progressive disclosure audit dashboard VNode tree")
    (:sym :name "renderAuditRadar" :tokens 1 :rationale "Renders VDOM container holding 6-axis radar SVG")
    (:sym :name "renderRadarSvg" :tokens 1 :rationale "Renders declarative 6-axis SVG radar chart markup")
    (:sym :name "renderBlastRadiusHeatmap" :tokens 1 :rationale "Renders blast-radius causal topology heatmap container")
    (:sym :name "renderFindingsList" :tokens 1 :rationale "Renders list of audit finding cards")
    (:sym :name "renderFindingCard" :tokens 1 :rationale "Renders single diagnostic finding card in progressive disclosure UI")
    (:sym :name "radarAxisPoints" :tokens 1 :rationale "Calculates SVG polygon points for 6-axis radar chart")
    (:sym :name "extractLensScores" :tokens 1 :rationale "Extracts health scores for all 6 canonical lenses from findings")
  ])

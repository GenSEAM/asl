(module asl-search/test
  :doc "Unit tests for SearXNG metasearch package"
  :export [run-tests]
  :import [(core/strings :as s)])

(defun run-tests [] -> Bool
  :doc "Runs search unit tests"
  (= (s/concat "http://" "example.com") "http://example.com"))
